local options = {
  ensure_installed = { "lua", "python", "sql"},

  highlight = {
    enable = true,
    use_languagetree = true,
    addittional_vim_regex_highlighting = true,
  },

  indent = { enable = true },
}

return options
